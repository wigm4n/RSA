package ru.hse;

class RealizationRSA {
    private int n;

    RealizationRSA(int n) {
        this.n = n;
    }

    public void congruential(int x) {
//        int m = 6075; // генерация псевдослучайных чисел в диапазоне значений от 0 до 100 (выбирается случайно m > 0
//        int a = 106; // множитель (выбирается случайно 0 <= a <= m)
//        int c = 1283; // инкрементирующее значение (выбирается случайно 0 <= inc <= m)
//        x = ((a * x) + c) % m; // формула линейного конгруэнтного метода генерации псевдослучайных чисел
//        return (x / double(m));
    }
}

